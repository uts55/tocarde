using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Image_Change_Shop : MonoBehaviour
{/*
    //게임시스템
    //public GameObject gameSystem;
    //public GameSystem gameSystem_script;

    //이미지 컴포넌트
    public Image icon;
    //부모 storage 스크립트
    public Unit_Select parent_select;
    //1성 아이콘 모음집
    public GameObject shop_image;


    // Start is called before the first frame update
    void Start()
    {
        //게임 시스템 스크립트
        //gameSystem_script = gameSystem.GetComponent<GameSystem>();
        icon = GetComponent<Image>();
        parent_select = transform.parent.GetComponent<Unit_Select>();
    }

    // Update is called once per frame
    void Update()
    {
        icon.sprite = shop_image.transform.GetChild(parent_select.unit_number - 1).GetComponent<SpriteRenderer>().sprite;
    }*/
}
